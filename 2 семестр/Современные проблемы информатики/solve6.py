import matplotlib.pyplot as plt  # pip install matplotlib

from math import sin, pi, sqrt
from random import uniform



def logspace(start, stop, num=50):
    """Возвращает список целых чисел, логарифмически распределённых
       от 10**start до 10**stop включительно, длиной num.
       Чтобы использовать np.logspace(2, 6, num=50, dtype=int) без numpy.
    """
    result = []
    for i in range(num):
        exponent = start + (stop - start) * i / (num - 1)
        value = int(10 ** exponent)
        result.append(value)
    return result



def buffon_trial(a, l):
    """Самый обычный метод отрабовки с двумя 'кси'."""
    phi = uniform(0, pi) # ξ₁
    x   = uniform(0, a)  # ξ₂
    return x <= l * sin(phi)

def buffon_simulation(N, a, l):
    """N бросков, возвращает P_exp и Δ."""
    hits = sum(buffon_trial(a, l) for _ in range(N))
    P_exp = hits / N
    P_theor = 2 * l / (a * pi)
    delta = abs(P_theor - P_exp) / P_theor
    return P_exp, delta

def main():
    # Отношение l/a задаётся преподавателем. Например, как на рисунке 2: l=1, a=2 -> l/a=0.5
    l = 1.0
    a = 2.0
    P_theor = 2 * l / (a * pi)

    # Массив значений N от 100 до 1_000_000 (логарифмическая шкала)
    N_values = logspace(2, 6, num=50)
    print("N space:", N_values)
    deltas = []

    for N in N_values:
        _, delta = buffon_simulation(N, a, l)
        deltas.append(delta)
        print(f"N = {N:7d}  Δ = {delta:.6f}")

    # График
    plt.figure(figsize=(10, 5))
    plt.plot(N_values, deltas, 'o-', markersize=3, label='Эксперимент')

    # Теоретическое убывание ~ 1/sqrt(N) (подгоним коэффициент по первой точке)
    k = deltas[0] * sqrt(N_values[0])
    theory = tuple(k / sqrt(N_values[i]) for i in range(len(N_values)))
    plt.plot(N_values, theory, 'r--', label=r'$\sim 1/\sqrt{N}$')

    plt.xscale('log')
    plt.yscale('log')
    plt.xlabel('Число бросков N')
    plt.ylabel('Нормированное отклонение Δ')
    plt.title(f'Задача Бюффона (l/a = {l/a:.1f}), теоретическая P = {P_theor:.4f}')
    plt.legend()
    plt.grid(True, which='both', ls='--')
    plt.tight_layout()
    plt.savefig('buffon_delta_vs_N.png', dpi=200)
    plt.show()



if __name__ == "__main__":
    main()



"""
1. В чём состоит классическое определение вероятности?

   Классическое определение вероятности: P(A) = m/n, где n — общее число
   равновозможных элементарных исходов, m — число исходов, благоприятствующих
   событию A. Применимо только для конечных пространств элементарных событий.

2. Как определяется вероятность в геометрическом подходе?

   Это просто метод Монте-Карло с отбраковкой (rejection sampling).
   В геометрическом подходе вероятность события равна отношению меры области,
   благоприятствующей событию, к мере всей области: P = мера(g)/мера(G), где
   G — область, в которую наудачу бросается точка, g — подобласть, попадание
   в которую означает наступление события. Мера может быть длиной, площадью,
   объёмом и т.д.

3. Чем псевдослучайные числа отличаются от случайных?

   Псевдослучайные числа генерируются детерминированным алгоритмом; они
   воспроизводимы и имеют конечный период. Истинно случайные числа получаются
   из физических процессов (шум, распад) и невоспроизводимы ОБЫЧНЫМ компьютером.

4. Назовите причины, по которым в компьютерном моделировании используются псевдослучайные числа.

   - быстрота генерации;
   - воспроизводимость результатов (удобно для отладки);
   - не требуется специальное оборудование;
   - при правильном выборе алгоритма обладают хорошими статистическими
     свойствами.

5. Возможен ли генератор истинных случайных чисел?

   Да, генераторы истинных случайных чисел существуют. Они основаны на
   физических явлениях (тепловой шум, квантовые эффекты), но работают
   медленнее и требуют аппаратной поддержки.

6. Возможно ли исполнение имитационной модели задачи Буффона без помощи компьютера?

   Да, можно физически бросать иглу на разлинованную бумагу и подсчитывать
   пересечения. Однако для получения приемлемой точности требуется очень
   большое число бросков, что делает ручное исполнение крайне трудоёмким.

7. Может ли ошибка Δ, полученная при имитации 1000000 бросков оказаться больше, чем ошибка полученная при имитации 1000 бросков?

   Да, может. Экспериментальная вероятность P_exp = k/N — случайная величина
   с распределением, близким к нормальному N(P, P(1-P)/N). Тогда ошибка
   Δ = |P_exp - P|/P также случайна. Хотя её типичный масштаб убывает как
   1/√N, в любом конкретном опыте из-за дисперсии может реализоваться
   значение, превышающее ошибку при меньшем N. Например, если при N=1000
   повезло и P_exp оказалось близко к P, а при N=1000000 случайно
   отклонилось чуть дальше (в пределах своего, пусть и меньшего, разброса),
   то Δ(10^6) > Δ(10^3). Это нормальное поведение случайной величины.

   НА ПРАКТИКЕ!) Мы этот эффект уже наблюдаем при 120 бросках (смотрите рисунок 'buffon_delta_vs_N.png').

8. Каково наиболее вероятное поведение зависимости Δ(N)?

   Наиболее вероятное поведение — убывание пропорционально 1/√N. В
   логарифмическом масштабе график представляет собой прямую с наклоном
   почти –1/4 (124/-31 пиксель, смотрите рисунок 'buffon_delta_vs_N.png').

9. Объяснить поведение зависимости Δ(N) при N < 100.

   При N < 100 оценка вероятности крайне неустойчива: дисперсия велика,
   поэтому наблюдаются сильные и нерегулярные колебания Δ. С ростом N
   флуктуации сглаживаются.
"""
