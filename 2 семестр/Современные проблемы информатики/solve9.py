import matplotlib.pyplot as plt  # pip install matplotlib

from sim_core.schemes import build_single_server, build_three_servers



def run_and_collect(builder, total_time=500, dt=0.5):
    """
    Запускает моделирование, возвращает time_axis и список длин очереди.
    Для многоканальной — суммирует очереди всех ServiceDevice.
    """
    runner, history = builder.run_with_logging()
    # history имеет структуру: {device: {"queue_length": [...]}} для каждого блока с queue_length

    time_axis = []
    steps = int(total_time / dt)
    for _ in range(steps):
        runner(dt)
        time_axis.append(builder.engine.time)

    queue_lists = []
    for props in history.values(): # т.к. в нынешней реализации ТОЛЬКО терминалы обслуживания имеют параметр queue_length, то keys() нас не интересует
        if "queue_length" in props:
            queue_lists.append(props["queue_length"])
    builder.stats()

    return time_axis, queue_lists



def main():
    TOTAL_TIME = 10000
    DT = 1

    b1 = build_single_server()
    t1, q1s = run_and_collect(b1, TOTAL_TIME, DT)

    b3 = build_three_servers()
    t3, q3s = run_and_collect(b3, TOTAL_TIME, DT)

    plt.figure(figsize=(12, 6))

    plt.subplot(1, 2, 1)
    for q1 in q1s:
        plt.plot(t1, q1)
    plt.xlabel("Модельное время")
    plt.ylabel("Длина очереди")
    plt.title("Одноканальная СМО (μ=0.9)")
    plt.grid(True)

    plt.subplot(1, 2, 2)
    for i, q3 in enumerate(q3s):
        plt.plot(t3, q3, label=f"q{i}")
    total_queue = [sum(vals) for vals in zip(*q3s)]
    plt.plot(t3, total_queue, label=f"q_total")
    plt.legend()
    plt.xlabel("Модельное время")
    plt.ylabel("Длина очереди")
    plt.title("Трёхканальная СМО (μ=0.3×3)")
    plt.grid(True)

    plt.tight_layout()
    plt.savefig("sim_queues.png", dpi=200)
    plt.show()

if __name__ == "__main__":
    main()



"""
1. Что такое система массового обслуживания?

   Система массового обслуживания (СМО) – это совокупность обслуживающих
   приборов, входного потока требований и дисциплины обслуживания,
   предназначенная для выполнения однородных операций над поступающими
   требованиями. Примеры: кассы, колл‑центры, серверы.

2. Привести примеры объектов, моделируемых системами массового обслуживания?

   - кассы в супермаркетах и вокзалов;
   - телефонные станции;
   - серверы обработки веб‑запросов;
   - производственные конвейеры;
   - очереди на погрузку/разгрузку транспорта.

3. Чем характеризуется состояние системы массового обслуживания?

   Состояние СМО обычно задаётся числом требований, находящихся в системе
   (в очереди и на обслуживании). Иногда дополнительно фиксируют число
   занятых приборов и длину очереди отдельно.

4. Чему равна вероятность того, что требование будет обслужено, если количество
   мест в очереди СМО бесконечно?

   При бесконечной очереди и стационарном режиме (ρ < 1) вероятность
   обслуживания равна 1 – любое требование рано или поздно будет обслужено.
   Если ρ ≥ 1, очередь неограниченно растёт, время ожидания становится
   бесконечно большим, но формально требование всё равно принимается в систему
   и будет обслужено когда‑нибудь (вероятность потери равна нулю, так как
   очередь не ограничена).

5. Возможны ли значения коэффициента загруженности СМО больше, чем единица?

   Да, ρ = λ/μ может быть больше 1. Это означает, что входной поток
   интенсивнее, чем суммарная пропускная способность приборов. Система
   перегружена, очередь неограниченно растёт.

6. Пусть интенсивность обслуживания составляет 3 требования в минуту.
   Какой интервал времени обслуживания требования более вероятен:
   от 10 до 15 секунд, или от 25 до 30 секунд?

   При экспоненциальном законе распределения времени обслуживания с
   интенсивностью μ = 3 мин⁻¹ = 0.05 с⁻¹ среднее время равно 20 с.
   Плотность вероятности f(t) = μ·e^(–μt) убывает с ростом t. Поэтому
   вероятность попасть в более ранний интервал [10, 15] с выше, чем в
   [25, 30] с. Количественно:
   P(10≤t≤15) = e^(–0.5) – e^(–0.75) ≈ 0.6065 – 0.4724 = 0.1341,
   P(25≤t≤30) = e^(–1.25) – e^(–1.5) ≈ 0.2865 – 0.2231 = 0.0634.
   Ответ: интервал от 10 до 15 секунд более вероятен.
"""
